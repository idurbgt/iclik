# Blokir akses langsung ke file PHP internal (hanya dipanggil via require)
# Apache 2.4+
Require all denied

# Fallback Apache 2.2
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
